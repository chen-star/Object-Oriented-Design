package atm;

/**
 * @author : alexchen
 * @created : 9/14/20, Monday
 **/
public class CheckingAccount extends Account {
    public CheckingAccount(Card card, int accountId, int balance) {
        super(card, accountId, balance);
    }
}
